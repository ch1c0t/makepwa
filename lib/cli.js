// Generated by CoffeeScript 2.5.1
(function() {
  var build, create, printHelp, watch;

  global.CWD = process.cwd();

  global.SRC = `${CWD}/src`;

  global.DIST = `${CWD}/dist`;

  ({build} = require('./build'));

  ({watch} = require('./watch'));

  ({create} = require('./create'));

  exports.run = function() {
    var _agn, _node, command, name;
    [_node, _agn, command] = process.argv;
    switch (command) {
      case 'build':
        return build();
      case 'watch':
        return watch();
      case 'new':
        name = process.argv[3];
        return create(name);
      default:
        return printHelp();
    }
  };

  printHelp = function() {
    return console.log('printHelp');
  };

}).call(this);
