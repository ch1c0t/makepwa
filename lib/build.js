// Generated by CoffeeScript 2.5.1
(function() {
  var build, buildDeps, buildMainScript, buildManifest, buildPages, buildSWRegistration, buildStyles, buildWorkers, ensureDirExists;

  ({ensureDirExists} = require('./util'));

  ({buildDeps} = require('./build/js/deps'));

  ({buildMainScript} = require('./build/js/scripts/main'));

  ({buildSWRegistration} = require('./build/js/scripts/register_sw'));

  ({buildWorkers} = require('./build/js/workers'));

  ({buildPages} = require('./build/pages'));

  ({buildStyles} = require('./build/styles'));

  ({buildManifest} = require('./build/manifest'));

  build = async function() {
    ensureDirExists(DIST);
    await Promise.all([buildDeps(), buildMainScript(), buildSWRegistration(), buildStyles(), buildManifest()]);
    await buildPages();
    return buildWorkers();
  };

  module.exports = {build, buildPages, buildStyles, buildMainScript, buildSWRegistration, buildDeps, buildWorkers, buildManifest};

}).call(this);
