// Generated by CoffeeScript 2.5.1
(function() {
  var build, buildDeps, buildIcons, buildMainScript, buildManifest, buildPages, buildSWRegistration, buildStyles, buildWorkers, ensureDirExists;

  ({ensureDirExists} = require('./util'));

  ({buildDeps} = require('./build/js/deps'));

  ({buildMainScript} = require('./build/js/scripts/main'));

  ({buildSWRegistration} = require('./build/js/scripts/register_sw'));

  ({buildWorkers} = require('./build/js/workers'));

  ({buildIcons} = require('./build/icons'));

  ({buildPages} = require('./build/pages'));

  ({buildStyles} = require('./build/styles'));

  ({buildManifest} = require('./build/manifest'));

  build = async function() {
    ensureDirExists(DIST);
    await Promise.all([buildDeps(), buildMainScript(), buildSWRegistration(), buildIcons(), buildStyles(), buildManifest()]);
    await buildPages();
    return buildWorkers();
  };

  module.exports = {build, buildPages, buildStyles, buildMainScript, buildSWRegistration, buildDeps, buildWorkers, buildIcons, buildManifest};

}).call(this);
