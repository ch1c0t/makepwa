// Generated by CoffeeScript 2.5.1
(function() {
  var createIcons, createManifest, createPackageFile, createPages, createSWRegistration, createScripts, createSrc, createStyles, createWorkers, exec, fs;

  fs = require('fs');

  ({exec} = require('child_process'));

  ({createManifest, createWorkers, createSWRegistration} = require('./common'));

  exports.createProject = function({name, dir}) {
    var spec;
    spec = {
      name: name,
      version: '0.0.0',
      scripts: {
        start: 'makepwa watch',
        build: 'makepwa build'
      },
      devDependencies: {
        makepwa: VERSION
      }
    };
    createPackageFile({spec, dir});
    createSrc({name, dir});
    console.log("Running 'npm install'");
    return exec('npm install', {
      cwd: dir
    });
  };

  createPackageFile = function({spec, dir}) {
    var source;
    source = JSON.stringify(spec, null, 2);
    return fs.writeFileSync(`${dir}/package.json`, source);
  };

  createSrc = function({name, dir}) {
    var src;
    src = `${dir}/src`;
    fs.mkdirSync(src);
    createPages(src);
    createStyles(src);
    createScripts(src);
    createWorkers(src);
    createIcons(src);
    return createManifest({name, src});
  };

  createPages = function(src) {
    var dir, source;
    dir = `${src}/pages`;
    fs.mkdirSync(dir);
    source = `doctype html
html
  head
    title Title

    meta(charset="utf-8")
    meta(http-equiv="x-ua-compatible" content="ie=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")

    link(rel="manifest" href="/manifest.webmanifest")
    link(rel="stylesheet" href="/styles/main.css")
    script(src="/scripts/main.js")
  body
    #app`;
    return fs.writeFileSync(`${dir}/index.pug`, source);
  };

  createStyles = function(src) {
    var dir, source;
    dir = `${src}/styles`;
    fs.mkdirSync(dir);
    source = `body
  background-color: white`;
    return fs.writeFileSync(`${dir}/main.sass`, source);
  };

  createScripts = function(src) {
    var dir;
    dir = `${src}/scripts`;
    fs.mkdirSync(dir);
    fs.writeFileSync(`${dir}/main.coffee`, `require './register_service_worker.coffee'

console.log 'from main'`);
    return createSWRegistration(dir);
  };

  createIcons = function(src) {
    var dir;
    dir = `${src}/icons`;
    fs.mkdirSync(dir);
    return fs.copyFile(`${__dirname}/icon.192x192.png`, `${src}/icons/icon.192x192.png`, function(error) {
      if (error) {
        throw error;
      }
    });
  };

}).call(this);
