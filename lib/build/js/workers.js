// Generated by CoffeeScript 2.5.1
(function() {
  var buildCustomSW, buildDefaultSW, fs, runWebpack;

  fs = require('fs');

  ({runWebpack} = require('./common'));

  ({buildDefaultSW} = require('./sw/default'));

  exports.buildWorkers = function() {
    var file;
    file = `${SRC}/workers/sw.coffee`;
    if (fs.existsSync(file)) {
      return buildCustomSW(file);
    } else {
      if (COMMAND === 'build') {
        return buildDefaultSW();
      }
    }
  };

  buildCustomSW = function(file) {
    return runWebpack({
      entry: file,
      output: `${DIST}/sw.js`
    });
  };

}).call(this);
