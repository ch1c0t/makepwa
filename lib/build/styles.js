// Generated by CoffeeScript 2.5.1
(function() {
  var ensureDirExists, failIfDirNotExists, sass;

  sass = require('sass');

  ({failIfDirNotExists, ensureDirExists} = require('../util'));

  exports.buildStyles = async function() {
    var result, sourceDir, targetDir;
    sourceDir = `${SRC}/styles`;
    failIfDirNotExists(sourceDir);
    result = sass.renderSync({
      file: `${sourceDir}/main.sass`
    });
    targetDir = `${DIST}/styles`;
    ensureDirExists(targetDir);
    return (await IO.write(`${targetDir}/main.css`, result.css.toString()));
  };

}).call(this);
