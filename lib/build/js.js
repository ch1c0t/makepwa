// Generated by CoffeeScript 2.5.1
(function() {
  var YAML, buildScripts, buildWorkers, ensureDirExists, handleWebpackErrors, readFileSync, webpack, writeFileSync;

  ({readFileSync, writeFileSync} = require('fs'));

  webpack = require('webpack');

  YAML = require('yaml');

  ({ensureDirExists} = require('../util'));

  handleWebpackErrors = function(error, stats) {
    var info;
    if (error) {
      console.error(error.stack || error);
      if (error.details) {
        console.error(error.details);
      }
      return;
    }
    info = stats.toJson();
    if (stats.hasErrors()) {
      console.error(info.errors);
    }
    if (stats.hasWarnings()) {
      return console.warn(info.warnings);
    }
  };

  buildScripts = function() {
    var conf, load_coffee, sourceDir, targetDir;
    sourceDir = `${SRC}/scripts`;
    failIfDirNotExists(sourceDir);
    targetDir = `${DIST}/scripts`;
    ensureDirExists(targetDir);
    load_coffee = {
      test: /\.coffee$/,
      use: 'coffee-loader'
    };
    conf = {
      mode: 'production',
      entry: `${sourceDir}/main.coffee`,
      output: {
        path: targetDir,
        filename: 'main.js'
      },
      module: {
        rules: [load_coffee]
      }
    };
    return webpack(conf, handleWebpackErrors);
  };

  buildWorkers = function() {
    var conf, load_coffee, sourceDir;
    sourceDir = `${SRC}/workers`;
    failIfDirNotExists(sourceDir);
    load_coffee = {
      test: /\.coffee$/,
      use: 'coffee-loader'
    };
    conf = {
      mode: 'production',
      entry: `${sourceDir}/sw.coffee`,
      output: {
        path: DIST,
        filename: 'sw.js'
      },
      module: {
        rules: [load_coffee]
      }
    };
    return webpack(conf, handleWebpackErrors);
  };

  module.exports = {buildScripts, buildWorkers};

}).call(this);
