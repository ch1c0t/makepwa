// Generated by CoffeeScript 2.5.1
(function() {
  var YAML, buildCustomSW, buildDefaultSW, buildDeps, buildScripts, buildWorkers, coffee, ensureDirExists, existsSync, failIfDirNotExists, handleWebpackErrors, path, readFileSync, runWebpack, webpack, writeFileSync;

  ({readFileSync, writeFileSync, existsSync} = require('fs'));

  path = require('path');

  webpack = require('webpack');

  YAML = require('yaml');

  coffee = require('coffeescript');

  ({failIfDirNotExists, ensureDirExists} = require('../util'));

  handleWebpackErrors = function(error, stats) {
    var info;
    if (error) {
      console.error(error.stack || error);
      if (error.details) {
        console.error(error.details);
      }
      return;
    }
    info = stats.toJson();
    if (stats.hasErrors()) {
      console.error(info.errors);
    }
    if (stats.hasWarnings()) {
      return console.warn(info.warnings);
    }
  };

  runWebpack = function({entry, output}) {
    var conf, dir, load_coffee, name;
    dir = path.dirname(output);
    name = path.basename(output);
    load_coffee = {
      test: /\.coffee$/,
      use: 'coffee-loader'
    };
    conf = {
      mode: 'production',
      entry: entry,
      output: {
        path: dir,
        filename: name
      },
      module: {
        rules: [load_coffee]
      }
    };
    return webpack(conf, handleWebpackErrors);
  };

  buildDeps = function() {
    var entry, file, generateWebpackEntry, spec, targetDir;
    file = `${SRC}/deps.yml`;
    if (existsSync(file)) {
      spec = YAML.parse(readFileSync(file, 'utf-8'));
      generateWebpackEntry = function(spec) {
        var entry, imports, makeVariable, reacts, source;
        ensureDirExists('/tmp/makepwa');
        entry = '/tmp/makepwa/deps.js';
        makeVariable = function(string) {
          return string.replace('-', '').replace('@', '').replace('/', '');
        };
        imports = Object.keys(spec['import']).map(function(dep) {
          var variable;
          variable = makeVariable(dep);
          return `import ${variable} from '${CWD}/node_modules/${dep}'
_deps_['${dep}'] = ${variable}`;
        }).join('\n');
        reacts = Object.keys(spec['react']).map(function(dep) {
          var components, rawImport, toFROM, variable;
          variable = makeVariable(dep);
          components = Object.keys(spec['react'][dep]).map(function(string) {
            return {
              original: string,
              raw: `Raw_${string}_from_${variable}`
            };
          });
          rawImport = components.map(function({original, raw}) {
            return `${original} as ${raw}`;
          }).join(', ');
          toFROM = components.map(function({original, raw}) {
            return `_deps_['${dep}']['${original}'] = wrap ${raw}`;
          }).join('\n');
          return `_deps_['${dep}'] = {}
import { ${rawImport} } from '${CWD}/node_modules/${dep}'

${toFROM}`;
        }).join('\n');
        source = `_deps_ = {}
window.FROM = (string) ->
  _deps_[string]

${imports}

import { wrap } from '${CWD}/node_modules/wrapjsx'
${reacts}`;
        writeFileSync(entry, coffee.compile(source));
        return entry;
      };
      targetDir = `${DIST}/scripts`;
      ensureDirExists(targetDir);
      entry = generateWebpackEntry(spec);
      return runWebpack({
        entry,
        output: `${targetDir}/deps.js`
      });
    }
  };

  buildScripts = function() {
    var sourceDir, targetDir;
    sourceDir = `${SRC}/scripts`;
    failIfDirNotExists(sourceDir);
    targetDir = `${DIST}/scripts`;
    ensureDirExists(targetDir);
    return runWebpack({
      entry: `${sourceDir}/main.coffee`,
      output: `${targetDir}/main.js`
    });
  };

  buildWorkers = function() {
    var customSW;
    customSW = `${SRC}/workers/sw.coffee`;
    if (existsSync(`${SRC}/workers/sw.coffee`)) {
      return buildCustomSW();
    } else {
      if (COMMAND === 'build') {
        return buildDefaultSW();
      }
    }
  };

  buildCustomSW = function() {
    var sourceDir;
    sourceDir = `${SRC}/workers`;
    failIfDirNotExists(sourceDir);
    return runWebpack({
      entry: `${sourceDir}/sw.coffee`,
      output: `${DIST}/sw.js`
    });
  };

  buildDefaultSW = function() {
    return console.log('buildDefaultSW');
  };

  module.exports = {buildDeps, buildScripts, buildWorkers};

}).call(this);
